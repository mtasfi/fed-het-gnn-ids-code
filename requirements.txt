# Core
numpy>=1.24
pandas>=2.0
pyarrow>=14
pyyaml>=6.0
scikit-learn>=1.3
matplotlib>=3.7
tabulate>=0.9          # markdown tables in E0 / probe reports
jinja2>=3.1            # pandas LaTeX tables

# Capture parsing (E0)
dpkt>=1.9.8

# Models
torch>=2.1
torch-geometric>=2.5
transformers>=4.48     # ModernBERT support
peft>=0.13

# Baselines
xgboost>=2.0
imbalanced-learn>=0.12

# Tracking (mirror only; CSVs are the record)
comet_ml>=3.40

# Optional
# kagglehub            # dataset.source.kind=kagglehub
# B2 (FedGATSage rerun) additionally needs the Fed_GNN requirements:
#   networkx python-louvain leidenalg igraph joblib + pyg-lib (for its LinkNeighborLoader)

# Tests
pytest>=7
